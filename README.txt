Casino Atlas static website package.
Open index.html to preview the homepage. All pages use folder-style URLs.

Affiliate/referral setup:
- All promotional CTA links now point to /go/.
- Edit the affiliate destination in one place only: /go/index.html.
- In that file, change the AFFILIATE_URL value inside the script.
- The old /login-and-play/ page is kept only as a fallback redirect to /go/.
